# Canonical public URL of the site (no trailing slash).
# Used for metadataBase, canonical URLs, sitemap, robots and JSON-LD.
NEXT_PUBLIC_SITE_URL=https://devfestogbomoso.com

# Optional: Google Search Console HTML-tag verification token (the content="…" value).
GOOGLE_SITE_VERIFICATION=
